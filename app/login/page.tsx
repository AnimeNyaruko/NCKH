import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Login',
};

export default function Page() {
	return <main className="w-full h-full flex flex-col items-center justify-center">Xin chào</main>;
}
